
function concatenate() { // creating a function
    var a= 'I LIKE';    // assigning string value
        a += 'CODING now'; // concatenating
        a += ',It is fun!'; // concatenating
        document.getElementById("concatenate").innerHTML= a; //assigning variable a to the html elements with concatenate id
}